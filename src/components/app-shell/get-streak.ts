/**
 * Current day streak — drives the tab header's flame chip.
 *
 * Stubbed until `workout_sessions` exists (SPEC.md milestone 2). Replace
 * with a real query once that table is live; the header doesn't change.
 */
export function getStreak(): number {
  return 4;
}
