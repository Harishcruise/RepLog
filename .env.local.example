# Supabase — from Project Settings > API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only. Never expose to the client. From Project Settings > API
SUPABASE_SERVICE_ROLE_KEY=

# Optional: direct Postgres connection for migrations / tooling
# DATABASE_URL=
